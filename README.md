# Boxxy Flatpak Remote
Temporary Flatpak remote for Boxxy while Flathub submission is in progress.

## Add Remote
```
flatpak remote-add --if-not-exists --no-gpg-verify boxxy https://miifrommera.github.io/boxxy-flatpak-remote/repo
flatpak install boxxy play.mii.Boxxy
```
